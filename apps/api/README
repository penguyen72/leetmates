# Leetmates API

## Development

1. Copy the example .env file and set your environment variables:

```bash
cp .env.example .env
```

2. Modify the .env file to include your MongoDB credentials, API keys, and other necessary configuration.

3. Install Go dependencies:

```bash
go mod tidy
```

4. Now, you're ready to run the development server:

```bash
go bash cmd/server/main.go
```
